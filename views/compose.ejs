<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>

    <%- include("partials/header.ejs") %>
    <h2 style="text-align: center;">Start Writing</h2>

    <!-- display previous title and entry-->
    <form action="/submit" method="POST">
        <input type="hidden" name="postIndex" value="<%= locals.index !== undefined && locals.index !== null ? locals.index : '' %>">
        
        <label for="blog-entry-title">Title</label>
        <input type="text" name="blog-entry-title" id="blog-entry-title" class="input" 
            value="<%= locals.index !== undefined && locals.index !== null ? titles[index] : '' %>" 
            required>
        
        <label for="post">Post</label>
        <textarea id="post" name="post" rows="4" cols="50" class="input" required><%= locals.index !== undefined && locals.index !== null ? entries[index] : '' %></textarea>
        
        <button type="submit" id="submit-btn">Submit</button>
    </form>
    
   
</body>
</html>